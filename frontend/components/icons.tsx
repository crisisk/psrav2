import { LucideIcon, Zap } from 'lucide-react';

export type Icon = LucideIcon;

export const Icons = {
  logo: (props: any) => <Zap {...props} />,
  sparkles: (props: any) => <Zap {...props} />,
  spinner: (props: any) => <Zap {...props} />,
  // Add other icons as needed
};
